<nav>
    <img
        src="https://public.svsticky.nl/logos/logo_compact_outline_wit.png"
        alt="Logo SV Sticky"
    />
    <ul>
        <li><a href="#info">Over de introductie</a></li>
        <li><a href="#signup">Inschrijven</a></li>
        <li><a href="#theme">Thema</a></li>
        <li><a href="#sticky">Over Sticky</a></li>
        <li><a href="#FAQ">FAQ</a></li>
        <li><a href="#contact">Contact</a></li>
    </ul>
    <a id="nav-icon">&#9776;</a>
</nav>

<script>
    const toggleNav = () => document.querySelector("nav")!.classList.toggle("responsive");

    document.getElementById("nav-icon")!.addEventListener("click", _ => toggleNav());

    document.querySelectorAll("nav ul a").forEach(link => {
        link.addEventListener("click", _ => toggleNav());
    });
</script>

<style lang="scss">
    html {
        scroll-behavior: smooth;
    }

    nav {
        background: #197052;
        height: 4rem;
        padding: 1rem 10rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: calc(100vw - 20rem);

        img {
            height: 4rem;
            align-self: flex-start;
        }

        ul {
            align-self: flex-end;
            display: flex;
            list-style-type: none;
            gap: 2rem;
            padding: 0;
            

            li a {
                color: white;
                text-decoration: none;

                &:hover {
                    text-decoration: underline;
                }
            }
        }

        #nav-icon {
            display: none;
            font-size: 1.8rem;
            color: white;
            cursor: pointer;
        }
    }

    @media screen and (max-width: 768px) {
        nav {
            padding: 1rem 1.5rem;
            justify-content: space-between;
            width: calc(100vw - 3rem);

            ul {
                display: none;
                flex-direction: column;
                gap: 1rem;
                width: 100%;
            }

            #nav-icon {
                display: block;
                text-decoration: none;
                margin: auto 0;
            }
        }

        nav.responsive ul {
            position: absolute;
            top: 6rem;
            left: 0;
            padding: 1rem;
            display: flex;
            z-index: 100;
            background: #197052;
            margin: 0;
            width: calc(100vw - 2rem);
        }
    }
</style>
